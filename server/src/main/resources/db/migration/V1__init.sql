-- Initial migration placeholder.
-- Real schema lands per-feature; see docs/specs/<feature>.md for owning context.
-- pgvector extension is enabled here so context migrations can declare vector columns.
CREATE EXTENSION IF NOT EXISTS vector;

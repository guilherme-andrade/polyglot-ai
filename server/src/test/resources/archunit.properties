# During scaffolding the bounded-context packages are empty. Allow rules whose
# `that(...)` clause currently matches no classes so the suite stays green until
# the first real feature lands. Tighten to `true` once contexts contain code.
archRule.failOnEmptyShould=false
